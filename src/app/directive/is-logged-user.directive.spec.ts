import { IsLoggedUserDirective } from './is-logged-user.directive';

describe('IsLoggedUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLoggedUserDirective();
    expect(directive).toBeTruthy();
  });
});
