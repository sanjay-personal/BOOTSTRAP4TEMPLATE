import { ClickdirectiveDirective } from './clickdirective.directive';

describe('ClickdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
